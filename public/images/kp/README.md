# Фото для КП (коммерческое предложение)

Изображения извлечены из **КП ТОО G&R Group (14) 27.02.2026.pdf** (команда `pdfimages`).

**Используемые в блоке «Цифровая экосистема»:** `intercom.jpg`, `camera.jpg`, `software.jpg`.

Оригинал PDF также доступен по ссылке: `/docs/kp-grgroup.pdf` (копия в `public/docs/`).

Остальные файлы `kp-XXX.*` — все извлечённые из PDF картинки; при необходимости можно заменить `intercom.jpg` / `camera.jpg` / `software.jpg` на другие кадры из этого набора.
